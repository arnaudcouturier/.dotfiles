-- NvChad's base46 themes, maintained independently of NvChad's plugin stack
-- (AvengeMedia fork). Lazy.nvim loads it on demand when a colorscheme is
-- picked, e.g. vim.cmd.colorscheme("base46-gruvchad").
return {
  "AvengeMedia/base46",
  lazy = true,
  opts = {},
}
