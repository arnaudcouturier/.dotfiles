# Herdr plugins installed by `dot init` and refreshed by `dot update`.
# One Herdr owner/repository[/subdirectory] source per line.
# None: the Annotate plugin was removed; add one per line to opt back in.
