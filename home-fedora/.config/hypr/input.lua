-- Personal input overrides for a Hyprland Lua config runtime.
-- Keep Caps Lock and the both-Shifts Caps Lock companion.
hl.config({
  input = {
    kb_options = "shift:both_capslock_cancel",
  },
})
